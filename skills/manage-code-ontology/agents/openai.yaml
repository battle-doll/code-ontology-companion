interface:
  display_name: "Code Ontology Companion"
  short_description: "Map code structure, dependencies, and change impact"
  default_prompt: "Use $manage-code-ontology to map this authorized Java/Spring or Python repository, answer a static dependency, injection, change-impact, or snapshot question with relationship evidence and adapter coverage, and use the default 2D or optional 3D view only when useful, without executing or changing target code."
